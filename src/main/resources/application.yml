spring:
  main:
    banner-mode: off
  jpa:
    properties:
      hibernate:
#        show_sql: true
#        format_sql: true
#        generate_statistics: true
        types:
          print:
            banner: false
  flyway:
    enabled: true
  datasource:
    url: jdbc:postgresql://localhost:5432/restorder
    username: postgres
    password: postgres

bot:
  client:
    username: test
    token: test
    web-hook-path: https://test.test
  user:
    enabled: false
    username: test
    token: test
    web-hook-path: https://test.test